<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.kinopractice">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".KinoApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.KinoPractice"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".presentation.views.add_theters.AddNewTheatersActivity"
            android:exported="false"
            />
        <activity
            android:name=".presentation.views.search_theters.SearchTheatersActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.views.add_film.AddFilmActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.views.search_films.SearchFilmsActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.views.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>